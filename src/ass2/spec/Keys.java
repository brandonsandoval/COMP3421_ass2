package ass2.spec;

public enum Keys {
    // List of keys that have a pressed and released state
    UNUSED, 
    SPACE, SHIFT, CONTROL,
    LEFT, RIGHT, UP, DOWN, 
    W, A, S, D, 
    T, C, G, V, R, X
}